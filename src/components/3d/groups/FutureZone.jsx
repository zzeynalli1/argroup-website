/**
 * Placeholder for a reserved-but-not-yet-modeled future discipline group
 * (interior, exterior, landscape, façade, electrical, wet fire protection,
 * lighting — see the reserved-zone coordinate table in
 * .claude/agents/3d-modeling-agent.md). Renders nothing on purpose: the
 * point of Phase 0 is reserving the *registry slot and coordinate space*,
 * not adding new visible geometry. Swap this component for a real one in
 * groups/index.js when that discipline actually gets modeled.
 */
export default function FutureZone() {
  return null
}
